<% flash.each do |key, value| %>
  <div class="alert alert-<%= key %> fade in hide-flash">
    <a href="#" data-dismiss="alert" class="close" aria-hidden="true">×</a>
    <ul>
      <li>
        <%= value %>
      </li>
    </ul>
  </div>
<% end %>
